<resources>

    <!-- Base application theme. -->
    <style name="ModernTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/textColor</item>
        <item name="android:textColorPrimary">@color/textColor</item>
        <item name="android:textColorSecondary">@color/textColor</item>
        <item name="android:textColorTertiary">@color/textColor</item>
        <item name="android:textColorPrimaryInverse">@color/textColor</item>
        <item name="android:textColorSecondaryInverse">@color/textColor</item>
        <item name="android:textColorTertiaryInverse">@color/textColor</item>
        <item name="android:itemBackground">@color/textColor</item>
    </style>

    <!--
    <style name="TaskbarTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!- Customize your theme here.
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/textColor</item>
    </style>
-->


</resources>
